# Required for the numpy simulator + evaluation + dashboard
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.30

# Optional: production PyTorch + Flower stack (if you want to wire the
# numpy simulator's logic to a real distributed setting).
# torch>=2.0
# flwr>=1.7
